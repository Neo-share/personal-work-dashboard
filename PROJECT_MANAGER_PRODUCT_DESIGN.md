# 项目管理平台产品设计说明书

> **SSOT**：产品目标与领域语义的**唯一来源**。实现进度见 [IMPLEMENTATION_STATUS.md](./IMPLEMENTATION_STATUS.md)，勿在本文件写实现状态。

关联计划文档：[项目管理平台方案](./PROJECT_MANAGER_PLATFORM_PLAN.md)

## 产品定位

项目管理平台是为你个人定制的项目管理驾驶舱，用于从“我的需求”出发管理当前工作区内的业务仓库、项目/应用、上下游协作、任务、里程碑、链接、风险和上线节奏。平台首版以本地 Web 应用形式独立运行，不侵入现有业务仓库。

## 管理对象粒度

平台采用分层管理模型：

- `我的工作台`：平台主入口，集中展示你需要推进、跟踪、验收、上线和关注风险的需求。
- `需求`：你负责管理的具体业务目标，是平台的核心被管理对象。
- `关联人员`：需求中的上游开发人员和下游交互人员，用于标记责任、沟通对象和阻塞环节，不作为平台的管理主入口。
- `仓库`：CodeLab 下的独立 Git 仓库，例如 `coin-h5`、`admin-portal`、`opman-web-new`。
- `项目/应用`：一个仓库内承载的业务项目；普通仓库通常一个仓库对应一个项目，monorepo 则可细分到 `apps/*`、业务模块或构建脚本对应的子项目。
- `任务/里程碑`：需求下的开发、联调、测试、提测、上线、回归等执行节点。

因此首版管理粒度不是只停留在仓库维度，而是以“我的需求”为主入口，把相关仓库、项目/应用、关联人员、任务、里程碑和风险串起来。你可以同时管理多个需求；一个需求可以关联多个上游和下游人员；一个仓库可以同时并行多个需求，一个需求也可以关联多个仓库。

## 我的工作台

平台首页和主要工作台应优先服务你个人的管理动作，而不是做成多人组织管理系统：

- 我的待推进需求：需要你持续跟进状态、风险和跨仓库协作的需求。
- 我的待确认事项：需要你推动产品、设计、接口、测试或发布确认的事项。
- 我的风险需求：接口未定、设计稿缺失、联调阻塞、测试资源不足、上线窗口不明确等风险。
- 我的上线窗口：近期要提测、UAT、上线和回归的需求。
- 我的沟通对象：每个需求关联的上游开发人员和下游交互人员，以及当前卡点归属。

其他人员不是平台的独立工作台用户，而是通过 `requirement_people` 与需求建立关系；仓库、项目/应用、链接、里程碑、风险等内容都通过需求为你的管理视角服务。

## 需求管理设计

当某个仓库接到一个新需求时，平台应把它登记为一条独立需求实例，再把相关仓库和项目/应用挂到这条需求上。需求与仓库、项目/应用都应按多对多关系设计。

建议需求模型命名为 `requirements`，核心字段包括：

- 需求名称：简短描述业务目标。
- 需求状态：待评审、开发中、联调中、提测中、待上线、已上线、已暂停。
- 优先级：高、中、低或自定义枚举。
- 主负责人：对需求推进结果负责的人员。
- 上游开发人员：前端、后端、客户端、公共组件、接口提供方等研发交付角色。
- 下游交互人员：产品、设计、测试、运营、业务验收、运维发布等需求流转和反馈角色。
- 开发分支：例如 `feature/member-benefits`。
- 目标版本：需求计划进入的版本或迭代。
- 关联链接：需求文档、Figma、YApi、GitLab/GitHub、提测单、发布单、飞书文档等。
- 环境信息：dev、sit、uat、prod、gray。
- 上线窗口：计划上线时间和实际上线时间。
- 风险与阻塞：接口未定、设计稿缺失、依赖项目未完成、测试资源不足等。
- 备注：补充说明和项目经理判断。

建议增加两个关系模型承载多对多关联：

- `requirement_repositories`：需求与仓库的关联关系，记录该仓库在需求中的职责、开发分支、环境、当前状态和风险。
- `requirement_apps`：需求与项目/应用的关联关系，记录该应用在需求中的业务范围、负责人、交付状态和备注。

这样可以同时支持“查看某个仓库当前并行了哪些需求”和“查看某个需求涉及哪些仓库、每个仓库进展如何”。

## 需求中心协作关系

平台应以需求为中心组织人员关系，而不是只在项目维度维护一个静态人员列表。每条需求都可以关联两类人员：

- `上游开发人员`：负责把需求转化为可交付代码和接口能力，包含前端、后端、客户端、公共组件维护者、接口提供方等。
- `下游交互人员`：负责需求确认、交互设计、测试验收、运营反馈、发布协同和业务落地，包含产品、设计、测试、运营、业务验收人、运维发布人员等。

建议增加 `requirement_people` 关系模型，用于描述某个人在某条需求中的方向、角色和状态：

- 需求 ID：关联 `requirements`。
- 人员 ID：关联 `people`。
- 管理角色：主负责人、参与人、关注人、验收人、发布协同人等。
- 协作方向：`upstream` 或 `downstream`。
- 角色类型：前端、后端、产品、设计、测试、运营、业务验收、运维等。
- 责任说明：该人员在需求中的具体职责。
- 当前状态：待参与、进行中、已完成、阻塞中。
- 备注：交付风险、沟通结论或特殊说明。

这样平台可以从你的工作台回答“我现在要推进哪些需求、哪些需求有风险、哪些需求卡在谁那里”，也可以进入需求详情后回答“谁在开发、谁在确认、谁在验收、当前卡在哪个人或哪一环”。

## 示例：一个需求关联多个仓库

假设你正在管理“新增会员权益页”需求，该需求同时涉及 `coin-h5`、`admin-portal` 和 `member-api` 三个仓库，平台中的管理结构如下：

```text
我的工作台
  我的需求：
    新增会员权益页
      状态：开发中
      优先级：高
      目标版本：2026-06 会员中心迭代
      关联链接：Figma / YApi / 需求文档 / 提测单 / 发布单
      环境：dev / sit / uat / prod
      里程碑：需求评审、开发完成、联调完成、提测、UAT 验收、上线、回归
      风险：接口字段未最终确认、会员等级规则需产品二次确认

      关联仓库：
        coin-h5
          职责：会员权益页前端开发
          开发分支：feature/member-benefits
          状态：开发中
          风险：移动端兼容性待验证
        admin-portal
          职责：会员权益配置后台
          开发分支：feature/member-benefits-config
          状态：待联调
          风险：配置字段需产品确认
        member-api
          职责：会员权益接口与等级规则
          开发分支：feature/member-benefits-api
          状态：开发中
          风险：接口字段未最终确认

      上游开发人员：前端张三、后台前端陈一、后端李四、会员接口提供方钱七
      下游交互人员：产品赵六、设计孙八、测试王五、运营周九、运维吴十
      任务：页面开发、后台配置开发、接口联调、埋点、兼容性测试、上线回归
```

该结构支持你先从“我的工作台”进入，查看自己正在管理的需求；进入需求后再统一查看多仓库交付、上下游人员、任务、里程碑、链接和风险。仓库不再是管理主轴，而是需求的关联对象。

## 典型查询与操作

平台接入对话式导航后，应支持以下自然语言操作：

- “查看 `coin-h5` 当前开发中的需求”
- “查看 `coin-h5` 当前并行了哪些需求”
- “查看我正在推进的所有需求”
- “查看我关注的高风险需求”
- “查看需要王五验收的需求”
- “打开新增会员权益页需求详情”
- “新增会员权益页需求涉及哪些仓库？”
- “新增会员权益页需求中每个仓库的开发分支是什么？”
- “这个需求关联了哪些 Figma 和 YApi？”
- “哪些需求还没提测？”
- “列出本周要上线的需求”
- “查看我负责推进的所有高风险需求”
- “查看新增会员权益页需求的上游开发人员”
- “查看新增会员权益页需求的下游交互人员”
- “哪些需求卡在下游验收环节？”
- “打开 `coin-h5` 的需求关系图”

这些指令最终应转换为项目列表筛选、需求详情跳转、关系图打开、编辑表单打开等结构化动作。

## 与平台方案的关系

本说明书补充产品侧管理粒度和需求管理示例；整体技术架构、数据采集范围、页面规划和实施阶段以 [项目管理平台方案](./PROJECT_MANAGER_PLATFORM_PLAN.md) 为准。
