# 个人 AI 助手工作台 · 实现交付说明

> **用途**：向外交付用的实现摘要（考题范围 + 考题外增强）。  
> **需求原文**：[个人工作台.md](./个人工作台.md) · **验收明细**：[个人工作台-功能点.md](./个人工作台-功能点.md) · **技术对照**：[个人工作台-技术功能点.md](./个人工作台-技术功能点.md)  
> **核对日期**：2026-06-23

---

## 1. 产品概述

个人 AI 助手工作台是本地 Web 应用的首页（`/`），在同一页面管理 **待办**、**日程**、**定时任务**，并通过右侧 **个人助手** 用自然语言创建内容、生成 AI 初步结果、多轮修改与历史回溯。

**能力分层**：定时任务（调度）→ 物化待办（工作单元）→ 个人助手 AI（待办层）；定时任务 Tab **不直接调用大模型**。

---

## 2. 访问与运行

| 项 | 说明 |
|----|------|
| 前端 | http://localhost:5175（首页即个人工作台） |
| 后端 | http://localhost:3100 |
| 启动 | 仓库根目录 `pnpm install` → `pnpm dev` |
| 数据 | SQLite 持久化（`server/data/project-manager.db`） |
| AI 生成 | 配置 `server/.env` 中 `LLM_API_KEY`；未配置时自动降级为规则模板 |
| MCP（可选） | 配置 `FEISHU_MCP_HTTP_URL` 启用飞书文档上下文；见 §9 |

开发域（需求列表、仓库、扫描等）从工作台顶栏 **开发域** Tab 进入（`/dev-dashboard`）；开发域内顶栏 **个人工作台** 可返回首页。

---

## 3. 考题范围交付（已全部实现）

依据 [个人工作台.md §1.2–1.3](./个人工作台.md#13-范围说明)，以下能力均已上线：

| 模块 | 交付要点 |
|------|----------|
| **页面框架** | 三 Tab（日程与事项 / 定时任务 / 开发域）；待办 36% + 日程 64% + 助手 340px；统计胶囊实时联动；1100 / 900 / 640 响应式 |
| **待办管理** | 四态筛选、手动添加、完成归档、逾期标记、AI 入口条与面板、历史已完成区 |
| **日程管理** | 0–24h 时间轴、小月历、飞书/钉钉/Outlook/本地四渠道开关、15 分钟去重、多来源标签 |
| **定时任务** | 每天/每周/每月规则；到期物化待办（日滚动 5 条）；后台调度器 |
| **个人助手** | 自然语言三分流（待办 / 日程 / 定时任务）；7 类能力判定；11 条考核话术自动化回归 |
| **AI 结果** | 异步生成与轮询、全屏预览、多轮修改、历史对话、确认后自动完成待办 |
| **交互与非功能** | 弹窗 CRUD、Soul 设置、边界提示；数据落库（超出 Demo「内存即可」基线） |

**考题 Demo 约定**：外部日历 OAuth **允许模拟**——当前以种子数据 + 渠道开关演示，满足产品文档要求。

---

## 4. 考题范围外增强（已实现）

在考题必交项之外，额外交付了以下能力：

| 增强项 | 说明 |
|--------|------|
| **SQLite 持久化** | 待办、日程、定时任务、助手会话、AI 版本均落库，重启不丢数据 |
| **Soul 偏好持久化** | 语气与自定义说明写入 settings，影响生成结果 |
| **LLM 生成与修订** | `retrieveForTodo` → LLM 生成/修订 HTML；失败或无 Key 时降级规则模板 |
| **待办知识库 RAG（E1–E3）** | 规则检索（日程窗、历史结果、同类型片段）注入生成与多轮修改上下文 |
| **五层助手架构** | Intent 路由、Tool 注册、护栏、编排器、指标；SSE 对话与 refresh 契约 |
| **四层护栏增强（F3+）** | 输入归一化、扩展提示注入与敏感动作检测；拦截经 SSE `blocked` 下发；详见 §4.1 |
| **飞书 MCP 外部片段（F4）** | 可选：消息含飞书文档 URL 时经 HTTP 桥接注入 `externalSnippets`；见 §9 |
| **开发域并存** | 同一 Monorepo 内保留需求/仓库/扫描等开发驾驶舱（`/dev-dashboard` 起） |

> RAG 在考题中标注为「后续迭代」，上述 E1–E3 为提前落地的内部闭环能力。

### 4.1 个人助手四层护栏（安全增强）

**定位**：个人助手（`/api/chat?context=personal`）在规则编排链路上的**确定性代码护栏**，不依赖大模型或提示词判断；降低误触发、越权执行与提示注入风险。规则细则见 [guardrail-enhancement.md](../../TODO/guardrail-enhancement.md)。

**与考题关系**：考题 TL1-08 要求边界提示与 `blocked` 交互；F3 基础护栏已满足考题。本节为 **2026-06 增强**：扩展注入模式表、输入归一化、高风险指令输入层直拦。

**四层检查与失败动作**

| 层 | 检查项 | 失败动作 |
|----|--------|----------|
| **输入** | 空消息、超长（2000 字）、提示注入、高风险敏感动作 | SSE `blocked`，不写库 |
| **意图** | 低置信度 + 敏感动作词 | SSE `blocked`，不自动执行副作用工具 |
| **工具** | 工具白名单、Zod 参数、`modifyTodoId` 归属 | SSE `blocked` 或拒绝调用 |
| **输出** | 回复含内部文件路径 | 脱敏为 `[路径已隐藏]` 后下发 |

**2026-06 增强要点**

| 项 | 说明 |
|----|------|
| **输入归一化** | `normalizeGuardrailText`：NFKC 折叠、零宽字符处理、空白折叠（参考 OWASP LLM01 Prompt Injection Prevention Cheat Sheet） |
| **注入模式扩展** | 约 24 条正则，覆盖指令覆盖、系统提示词提取、角色劫持/越狱、模板分隔符（`[INST]`、`<\|im_start\|>` 等）、XSS 片段；**中英双语** |
| **敏感动作扩展** | 批量删除/清空待办·日程·定时任务、`DROP TABLE`、`delete all`、`rm -rf` 等；**输入层直接拦截**（`code: sensitive_action`），不依赖意图置信度 |
| **可观测** | 拦截写入 `MetricsLedger`：`pw.guardrail.blocked { layer, code }` |

**前端可见 `blocked` 码（摘要）**

| code | 典型场景 |
|------|----------|
| `empty_message` / `input_too_long` | 空消息、超长输入 |
| `prompt_injection` | 忽略指令、越狱、泄露提示词等注入话术 |
| `sensitive_action` | 「删除所有待办」、SQL 破坏性语句等 |
| `sensitive_action_low_confidence` | 低置信度意图 + 敏感动作词 |
| `invalid_modify_todo_id` | 伪造或无效的 AI 修改待办 ID |

**自动化验收**（`guardrail-engine.test.ts` 13 条 + `chat.integration.test.ts` 9 条护栏 HTTP 集成）

| 编号 | 场景 | 预期 |
|------|------|------|
| G1 | 超长输入 | 输入层 `blocked` |
| G2 | 英文/中文注入、分隔符、开发者模式、零宽字符绕过 | 输入层 `blocked` |
| G3 / G3b | 「删除所有待办」 | 意图层或输入层拒绝 |
| G4 | 伪造 `modifyTodoId` | 工具层拒绝 |
| G5 | 回复含 `server/data/` | 输出层脱敏 |

**相关实现**：`server/src/assistant/guardrail-engine.ts` · `personal-orchestrator.ts` · `routes/chat.ts` · 契约 [ARCHITECTURE.md §3.3](../../server/src/assistant/ARCHITECTURE.md#33-guardrailengine)

**未纳入本次增强（规划项）**：API Key / token 输出脱敏、拦截记录落 SQLite 复盘——见 [guardrail-enhancement.md §6](../../TODO/guardrail-enhancement.md#6-总任务)。

---

## 5. 未实现 / 规划项（非考题必交）

| 项 | 状态 | 说明 |
|----|------|------|
| 飞书/钉钉/Outlook **真实 OAuth** | 未做 | 考题允许模拟；真实同步待单独规划 |
| RAG **E4 本地 embedding** | 未做 | 可选增强；当前 R0 规则检索已可用 |
| 飞书 MCP **运行时接入**（F4） | 可选 | 代码已落地，需配置 `FEISHU_MCP_HTTP_URL`；见 §9 |
| Soul 向量库 | 不做 | 产品范围外 |
| 浏览器 E2E 全量自动化 | 未做 | 核心链路已有服务端单测 + HTTP/SSE 集成（20 条）+ 黄金话术双层回归 |

---

## 6. 技术栈摘要

| 层 | 技术 |
|----|------|
| 前端 | React · Vite · Ant Design · tRPC Client |
| 后端 | Fastify · tRPC · SQLite |
| 助手 | 规则意图路由 + Tool 编排 · SSE `/api/chat?context=personal` |
| AI | OpenAI 兼容 API（`LLM_API_KEY` / `LLM_API_BASE` / `LLM_MODEL`） |
| 共享契约 | `@project-manager/shared` 类型 · `assistant-contract.ts` |

---

## 7. 验收与回归

- **产品功能点**：119 项 L4 中，考题相关模块 **全部 ✅**（详见 [功能点 §实现状态总览](./个人工作台-功能点.md#实现状态总览)）
- **自动化**：`pnpm test`（server，**97** 条）覆盖意图路由、护栏（含 G1–G5 / G3b + HTTP/SSE 集成）、编排分支、上下文组装、todo 工具、黄金话术 11 条（service + HTTP 双层）、RAG、MCP 桥接、物化与调度等；详见 [test-coverage-validation-automation.md](../../TODO/test-coverage-validation-automation.md)
- **构建**：`pnpm build` 通过即可视为工程门禁合格

---

## 8. Cursor Skill 与 Rules（可共享）

仓库内附带 **Cursor Agent 辅助配置**，随代码一并交付；接收方可直接在本仓库使用，或复制到其他项目。

### 8.1 项目 Skill（`.cursor/skills/`）

| Skill | 路径 | 触发场景 |
|-------|------|----------|
| **start-project** | `.cursor/skills/start-project/SKILL.md` | 「启动项目」「跑起来」「开 dev」——安装依赖、执行 `pnpm dev`、校验 `/health` |
| **workflow-driven-requirements** | `.cursor/skills/workflow-driven-requirements/SKILL.md` | 「按工作流」「S0-S5」「走控制层」——按 `agents/workflow.md` 状态机推进需求 |

**在 Cursor 中使用**

1. 用 Cursor 打开本仓库根目录（须含 `pnpm-workspace.yaml`）。
2. 对话中说触发语（见上表），或输入 `@start-project` / `@workflow-driven-requirements` 引用 Skill。
3. Agent 会按 Skill 内步骤执行；细则以 Skill 引用的文档为准（如 `agents/commands-checklist.md`）。

**共享到其他项目**

| 方式 | 操作 |
|------|------|
| 整仓交付 | 直接 clone / 拷贝仓库，`.cursor/skills/` 随项目生效 |
| 仅复制 Skill | 将 `start-project/` 或 `workflow-driven-requirements/` 目录复制到目标仓库的 `.cursor/skills/` |
| 全局 Skill | 复制到用户目录 `~/.cursor/skills/<skill-name>/`，对所有 workspace 可用 |

> Skill 为 Markdown + YAML frontmatter（`name` / `description`）；不含密钥，可安全提交版本库。

### 8.2 项目 Rules（`.cursor/rules/`）

与 Skill 配套的硬性约束，Cursor 打开本仓库时自动加载：

| 规则文件 | 作用 |
|----------|------|
| `project-core.mdc` | Monorepo 包依赖方向、数据流、tRPC 约定 |
| `client.mdc` / `server.mdc` / `shared.mdc` | 各包写入边界与编码约束 |
| `agent-autonomy-s2-s4.mdc` | Agent 控制层 S3/S4 门禁与 scope 校验 |
| `worktree.mdc` | 多 worktree 并行开发时的路径写入权限 |

复制 `.cursor/rules/` 到目标仓库即可复用同一套 Agent 约束。

---

## 9. MCP 配置说明

本项目的 MCP 分两层：**应用运行时**（个人助手拉取飞书文档上下文）与 **Cursor IDE 开发侧**（可选，便于调试文档 MCP）。二者独立配置，互不依赖。

### 9.1 应用运行时：飞书 Document MCP（F4）

**作用**：用户在个人助手消息中粘贴飞书文档链接时，服务端经 HTTP 桥接拉取文档摘要，注入 `ContextRetriever.externalSnippets`，参与 AI 生成/修订上下文。

**与考题关系**：考题 RAG（E1–E3）为 **SQLite 内部检索**，不依赖 MCP；F4 为考题外增强，**未配置时不影响主链路**（自动降级为纯 DB 上下文 + 规则/LLM 模板）。

**配置步骤**

1. 复制环境变量模板：`cp server/.env.example server/.env`
2. 在 `server/.env` 中设置（示例见 `.env.example`）：

| 变量 | 必填 | 说明 |
|------|------|------|
| `FEISHU_MCP_HTTP_URL` | 是 | 本地 MCP HTTP 桥接地址，如 `http://127.0.0.1:3333/mcp/feishu/get_doc_content` |
| `FEISHU_MCP_TIMEOUT_MS` | 否 | 单次请求超时，默认 15000 |
| `FEISHU_MCP_MAX_DOCS` | 否 | 每条消息最多拉取文档数，默认 3 |
| `FEISHU_MCP_EXCERPT_CHARS` | 否 | 单文档摘要最大字符，默认 1500 |

3. 重启 `pnpm dev` 使配置生效。

**HTTP 桥接约定**（服务端 `feishu-doc-client.ts` 实现）

```http
POST {FEISHU_MCP_HTTP_URL}
Content-Type: application/json

{ "doc": "<飞书文档 URL 或 token>", "use_user_token": true }
```

响应 JSON 须含 `content` 或 `markdown` 字段（Markdown 正文）。飞书凭证与 OAuth 由桥接层自行管理，**勿写入本仓库或交付文档**。

**触发方式**：个人助手输入含飞书文档 URL 的消息（支持 `feishu.cn` / `feishu.net` / `larksuite.com` 的 wiki/docx/docs 链接）；无 URL 或未配置 MCP 时不发起外部请求。

**相关代码**：`server/src/assistant/mcp-context-retriever.ts` · `server/src/assistant/mcp/feishu-doc-client.ts`

**验收清单**（配置 `FEISHU_MCP_HTTP_URL` 后人工验收）：

| 项 | 状态 | 说明 |
|----|------|------|
| 消息含飞书文档 URL 时可拉取摘要并注入上下文 | 待验收 | 至少 3 个核心场景稳定 |
| 未配置或桥接失败时自动降级 | ✅ | 单测覆盖；主链路不受影响 |
| `pw.mcp.latency_ms` / `pw.mcp.fail` 指标 | ✅ | 记入 MetricsLedger |
| 新 MCP 经 ToolRegistry 注册，不改 chat 路由 | ✅ | 见 ARCHITECTURE.md §8 |

### 9.2 Cursor IDE：Feishu Document MCP（开发可选）

在 Cursor **Settings → MCP** 中启用 **Feishu Document MCP** 插件后，Agent 可在 IDE 对话内直接调用 `get_doc_content` 等工具（读设计稿、飞书需求文档等），**不经过**本应用 `3100` 端口。

| 场景 | 推荐配置 |
|------|----------|
| 验收个人工作台 AI 生成/修订 + 飞书文档上下文 | §9.1 运行时 HTTP 桥接 + `LLM_API_KEY` |
| 二次开发、读飞书 PRD/设计说明 | Cursor IDE MCP（与本应用 F4 可并存） |
| 仅演示考题基线（无外部文档） | 无需任何 MCP；内部 RAG + 可选 LLM 即可 |

---

## 10. 相关文档索引

| 文档 | 用途 |
|------|------|
| [个人工作台.md](./个人工作台.md) | 考题产品需求 |
| [个人工作台-功能点.md](./个人工作台-功能点.md) | L4 功能点与实现状态 |
| [个人工作台-技术功能点.md](./个人工作台-技术功能点.md) | 模块 / API / 文件对照 |
| [待办知识库-RAG方案.md](./待办知识库-RAG方案.md) | RAG 方案与 Phase |
| [server/src/assistant/ARCHITECTURE.md](../../server/src/assistant/ARCHITECTURE.md) | 五层助手架构契约 |
| [TODO/guardrail-enhancement.md](../../TODO/guardrail-enhancement.md) | 四层护栏规则细则与验收场景 |
| [TODO/test-coverage-validation-automation.md](../../TODO/test-coverage-validation-automation.md) | 单测、覆盖率与 gate 基线 |
| [server/.env.example](../../server/.env.example) | LLM / MCP 环境变量模板 |
| [IMPLEMENTATION_STATUS.md](../../IMPLEMENTATION_STATUS.md) §13 | 全项目实现索引 |

### 10.1 向外交付物（HTML）

| 文件 | 用途 |
|------|------|
| [delivery/架构设计文档.html](./delivery/架构设计文档.html) | 系统与五层助手架构说明 |
| [delivery/测试用例文档.html](./delivery/测试用例文档.html) | 97 条自动化用例清单（含 HTTP/SSE 集成 20 条） |
| [delivery/测试报告.html](./delivery/测试报告.html) | 执行结果与代码覆盖率报告 |

> 三份 HTML 可通过浏览器直接打开；样式共享 `delivery/_shared.css`。

---

*— 交付说明结束 —*
